// Discounts are no longer capped by a manager-configurable setting — employees can
// apply any discount directly. Only discounts at or above this threshold require a
// manager-approved override before the sale can complete.
export const DISCOUNT_OVERRIDE_THRESHOLD_PERCENT = 50;
